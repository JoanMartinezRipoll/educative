<%= form_for(@shareholder) do |f| %>
  <% if @shareholder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shareholder.errors.count, "error") %> prohibited this shareholder from being saved:</h2>

      <ul>
      <% @shareholder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :trustee_id %><br>
    <%= f.number_field :trustee_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
